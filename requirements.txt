# Core dependencies (heavy ML packages — install system-wide or separately)
opencv-python
numpy
ultralytics
gpiozero

# Raspberry Pi only (install on Pi)
picamera2
adafruit-circuitpython-servokit

# Web interface
flask
flask-socketio
gunicorn
APScheduler